export const eventSettings = {
  // A list of events (by event name) that the game will start with scheduled.
  // Can include null spots to place random events in between specified ones
  initSchedule: [
      'prologue',
    ],
  maxTimelineLength: 200,
}